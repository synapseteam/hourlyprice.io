import { css } from "@emotion/react";

export const styles = {
  textArea: css`
    border: none;
    margin: 1px;
    background: #fff;
    min-width: 28px;

    &:disabled {
      border: none;
    }
  `,
};
